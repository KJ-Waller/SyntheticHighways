TODOS
* Make it so that HMM method uses entire G1 map and only keeps bounding box at the end
* Experiments need to be run using train/test set somehow
* Make more map tiles/patches, different difficulties
* More in-depth evaluation in ablation studies