TODOS
* Experiments need to be run using train/test set somehow
* Make more map tiles/patches, different difficulties
* Make sure rule-based mentions the error of using lat lon when computing the distance as mentioned in the algorithm